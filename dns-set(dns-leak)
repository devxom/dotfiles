##Solution 1 (Command Prompt)
1. Open the Command Prompt as admin
2. Use following command for the first DNS IP-Address

```powershel
    netsh interface ip add dns name="Ethernet" addr=8.8.4.4 index=1
```

3. Use following command for the second DNS IP-Address
```powershel
    netsh interface ip add dns name="Ethernet" addr=8.8.8.8 index=2
```
4. Use the following command to check the DNS

```powershel
    ipconfig /all
```
#Solution 2 (PowerShell)

1. Open the Windows PowerShell as Administrator
2. Use following command

```powershel
Set-DnsClientServerAddress -InterfaceAlias Ethernet -ServerAddresses 8.8.8.8
```



```powershel
    netsh interface ip add dns name="Cellular" addr=8.8.4.4 index=1
    netsh interface ip add dns name="Cellular" addr=8.8.8.8 index=2
    netsh interface ip add dns name="Cellular" addr=208.67.222.222 index=3
    netsh interface ip add dns name="Cellular" addr=208.67.220.220 index=4
    netsh interface ip add dns name="Cellular" addr=91.214.71.181 index=5
    netsh interface ip add dns name="Cellular" addr=193.183.98.154 index=6
```
